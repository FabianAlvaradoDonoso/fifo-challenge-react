export const ROOTS = {
  AUTH: '/auth',
  HOME: '/home',
  LANDING: '/',
}

// ----------------------------------------------------------------------

export const paths = {
  auth: {
    login: `${ROOTS.AUTH}/login`,
    register: `${ROOTS.AUTH}/register`,
  },
  home: `${ROOTS.HOME}`,
  landing: `${ROOTS.LANDING}`,
}
